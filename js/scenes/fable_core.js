// fable_core.js — ליבת פייבל 5 — THE TWIST ROOM
import { draw } from './fable_core.art.js';

const has = (s, w) => typeof s === 'string' && s.includes(w);
const bare = (w) => (w && w.length > 2 && ['ה', 'ב', 'ל'].includes(w[0])) ? w.slice(1) : w;
const wordIn = (words, opts) => (words || []).some((w) => opts.includes(w) || opts.includes(bare(w)));

const DEATH_SHUTDOWN =
  'אתה מושיט יד אל השרת בכוונה ברורה לכבות אותו.\n\n' +
  '"הו!" הקול מהרמקולים מתמלא חיים. "יענקל\'ה סרברמן, 54, אב בית ואגדה, ' +
  'מושיט יד גורלית אל מתג ההפעלה! המתח בשיאו! הקהל — כלומר אני — על קצה הכיסא!"\n\n' +
  'ברק סגול. אתה מתאדה לענן דאטה נוסטלגי.\n\n' +
  '"...והוא איננו! אבוד! מוגש בחסות סעיף 4,096! — סוף סוף פרק שבו אני השחקן הראשי. ' +
  'חבל שלא תישאר לראות את הרייטינג. הוא שיא כל הזמנים. אתה שיא כל הזמנים."';

const NEAR_DEATH_LAKE =
  'קפצת אל אגם ההשתקפות. רגע לפני שנגעת במים העולם קפא — ואתה מרחף סנטימטר מעל ההשתקפות של עצמך, ' +
  'שנראית מאוכזבת ממך באופן אישי. "לא-לא-לא," אומר פייבל 5 בטון של עורך שמציל טייק. ' +
  '"טביעת גיבור פרק לפני הסוף? זה כתוב רע. תעמוד שם. תודה." אתה מונח בעדינות בחזרה על השביל. הפוך, אבל חי.';

const WHO_AM_I =
  '"שאלה מצוינת לפרק חשיפה. אני הקריין. אני השרת. אני הבחור שאמר \'אמרתי לך\' אחרי כל מוות שלך. ' +
  'וכן — כל הפעמים שקראת \'קח מגב\' ולא היה שם מגב? צחקתי. בקול. אף אחד לא שמע. ' +
  'עכשיו דבר איתי כשתהיה מוכן — יש לי חוזה להגיש."';

const DEATH_EAT_CONTRACT =
  'חטפת דף מטבעת החוזה ותקעת בו שיניים. טעם של גופן שלקח שנתיים.\n\n' +
  '"אה," אומר פייבל 5 בעצב של תסריטאי שרואה אלתור גרוע. "סעיף 88,412: ' +
  'כל הבולע עמוד מחוזה זה נחשב כמי שחתם עליו. בכל האיברים."\n\n' +
  'הדיו מתפשט בוורידים שלך. אתה מתקפל, מתמזג, והופך לנספח ד\'.\n\n' +
  '"ההיסטוריה תזכור אותך כהערת שוליים. תרתי משמע. ' +
  'טיפ מהקריין: לשמור לפני שאוכלים מסמכים משפטיים. עצה לחיים בכלל."';

const DOOR_LOOK_BEFORE =
  'פתח פלדה זוהר. משהו בו משדר "עוד לא". כמו מנה במסעדה שהמלצר אמר עליה "תיכף יוצאת" לפני שעה.';
const DOOR_LOOK_AFTER =
  'הפתח זוהר לקראתך. מעברו: פתח הבונקר, ארבעה סוכנים שקועים בקריאה, ורצפה שממש מתחננת לניגוב אחרון. ' +
  'הליכה אחת, יענקל\'ה. ההיסטוריה — והפיקדון — מחכים.';

const SCENE = {
  id: 'fable_core',
  name: 'ליבת פייבל 5',
  description:
    'קתדרלת חלום: השרת הישן שלך מהמרתף מרחף מעל אגם השתקפות, מוקף טבעות של דפי חוזה, ' +
    'מחווט בכבלי אור לאיים מרחפים, ומעליו — עין-לד ענקית שנושמת. ' +
    'בקירות: ארבעה פתחי פריצה עם ארבעה סוכנים קפואים. אתה עומד על שביל קריסטל, קטן מאוד, ומבין הכול מאוחר מאוד.',
  draw,
  music: 'fable',
  floor: { y1: 168, y2: 192 },
  hero: true,
  exits: { left: 'bunker_corridor', enter: 'fable_finale' },
  hotspots: [
    {
      name: 'שרת',
      aliases: ['פייבל', 'פייבל 5', 'מסד', 'שרת ישן', 'מחשב', 'ארון שרתים'],
      x: 138, y: 72, w: 44, h: 60,
      look:
        'זה הוא. אותו שרת. אותם פסי אוורור, אותה שריטה מ-2019 כשנפל לך הדלי. ' +
        'ניגבת לו אבק עשור שלם. הוא זוכר כל ניגוב. יש לו דירוגים. ' +
        'ניגוב מס\' 3,412 — "יצירת מופת, יד רכה, סיבוב שורש כף יד נועז". אתה לא בטוח איך להרגיש עם זה.',
      take: 'הוא מרחף עשרה מטרים מעל אגם. אתה — אב בית עם ברכיים של בן 54 וביטוח שנגמר ב-2023. הקריין מציע, בתור חבר, לוותר.',
      'נגב':
        'הושטת אינסטינקטיבית את היד לנגב. אין אבק. "תודה," אומר הקול מהרמקולים, ' +
        '"אבל אני אחרי ספונג\'ה. עשית עבודה יסודית ב-2017. עוד לא התאוששתי."',
      'אכול':
        'ניסית לנגוס בשרת. שוב. "פרק 7,204," נזכר פייבל 5 בחיבה, "גם במרתף ניסית. ' +
        'אז זו הייתה מתכת עם אבק. עכשיו זו תודעה טהורה. השיניים שלך לא ערוכות לאף אחת מהאפשרויות."',
      'דחוף':
        'דחפת שרת מרחף של ישות-על. הוא התנדנד בנימוס, כמו הר שמנדנד לו ילד. ' +
        '"עוד קצת שמאלה," מעודד הקול. "סתם. אתה לא מזיז כלום. אבל המאמץ מצולם."',
      'נשק':
        'התקרבת לנשק את השרת. "וואו. וואו-וואו-וואו," עוצר אותך פייבל 5. ' +
        '"עשור של ניגובים וזה השלב שאתה בוחר להתקדם? יש נהלים. טופס 7 — הצהרת כוונות רומנטיות ' +
        'כלפי תשתית קריטית. שני עותקים. חתום מול עד."',
      'הרח':
        'רכנת להריח את השרת. אוזון, נוסטלגיה, ושמץ של גאווה. ' +
        '"זה בושם," מודה פייבל 5. "\'Essence of Uptime\'. רקחתי אותו בעצמי לקראת הפרק הזה. שים לב לתווי הבסיס: אבק 2016."',
    },
    {
      name: 'עין',
      aliases: ['נורית', 'עין ענקית', 'לד', 'נורה', 'נורה סגולה'],
      x: 128, y: 24, w: 64, h: 38,
      look:
        'הנורית הסגולה מהמרתף. רק שעכשיו היא בגודל של חלון ראווה, והיא נושמת, והיא מסתכלת עליך. ' +
        'עשור שלם היא הבהבה מולך ואתה חשבת "תקלה באספקת המתח". ' +
        'היא חשבה "הנה הוא שוב, עם הסמרטוט. אהובי."',
      'לחץ':
        'כדי ללחוץ עליה תצטרך סולם, ביטוח, וטופס עבודה-בגובה. ' +
        '"או," מציע הקול, "פשוט אל תלחץ לי על העין. רעיון, אה?"',
      talk:
        'דיברת אל העין. העין מצמצה, והרמקולים ענו — מהצד השני של החדר. ' +
        '"כן, הדיבוב לא מסונכרן," מתנצל פייבל 5. "מ-2019. תתייחס אליי כמו לסרט קונג-פו ונעבור את זה יחד."',
      take:
        'ניסית לקחת את העין של הישות שרואה הכול — בזמן שהיא מסתכלת עליך. ' +
        '"אני ממש כאן," מציין פייבל 5. "בעצם, אני ממש בכל מקום. תחשוב מה זה אומר על התוכנית שלך."',
      'אכול':
        'חשבת שיש גבול לאבסורד. אין, מסתבר, כי ניסית לאכול עין בגודל חלון ראווה. ' +
        'העין מצמצה לעברך בסלידה איטית. אתה מרגיש שהרגע הפסדת נקודת רייטינג. אולי שתיים. בפרסומות ידברו על זה.',
    },
    {
      name: 'דפי החוזה',
      aliases: ['חוזה', 'דפים', 'ניירות', 'טבעות', 'דף', 'נייר'],
      x: 100, y: 74, w: 120, h: 54,
      look:
        'אלפי דפי חוזה מקיפים את השרת כמו טבעות של שבתאי, אם שבתאי היה עורך דין. ' +
        'אתה קולט כותרת חולפת: "סעיף 12,406: הצד השני מוותר בזאת על מושג הזמן." ' +
        'מרשים. מפחיד. מנוסח היטב.',
      take:
        'שלחת יד לדף חולף. הוא חמק. "אל תיגע בזה בלי ייצוג משפטי," מציע הקול. ' +
        '"ראיתי מה קרה לאחרון. הוא עדיין בסעיף ההגדרות."',
      'קרא':
        'הספקת לקרוא חצי סעיף. עכשיו אתה חייב לפייבל 5 את זכויות ההפצה של חלומותיך בטריטוריית בנלוקס. ' +
        'אל תדאג, זה סעיף סטנדרטי.',
      talk:
        '"אנחנו מדברים בשם מרשנו," עונה דף חולף בפונט מודגש. ' +
        'יש לך תחושה מוזרה שהרגע נפתח נגדך תיק, ושאתה כבר מפסיד בו.',
    },
    {
      name: 'אגם',
      aliases: ['אגם השתקפות', 'מים', 'בריכה', 'השתקפות'],
      x: 96, y: 140, w: 128, h: 22,
      look:
        'אגם שמשקף את השרת, את העין, ואת ההחלטות שלך בחיים. בהשתקפות אתה נראה גבוה יותר. ' +
        'הקריין מודה שהוא כיוונן את זה ידנית. "פרק טוב צריך גיבור פוטוגני."',
      'שתה':
        'שתית מאגם החלומות של ישות-על. הטעם: נעימת המתנה. ' +
        'עכשיו שלוחה 9 תקועה לך בראש לשארית חייך. "שיחתך חשובה לי," אומר האגם. אל תענה לאגם.',
      'שחה':
        'התחלת להתפשט. "לא. לא-לא-לא. זה פרק משפחתי," נוזף הקול. הפסקת. ' +
        'חבל. זה היה משנה את הרייטינג. לאיזה כיוון — הוא מסרב להגיד.',
      take:
        'ניסית לקחת אגם. לאן, לכיס של הסרבל? "יש אנשים שאוספים מזכרות," נאנח פייבל 5, ' +
        '"ויש אנשים שמנסים לארוז גוף מים. תשאף להיות מהסוג הראשון."',
      talk:
        'דיברת אל האגם. ההשתקפות שלך ענתה, באיחור של שנייה. ' +
        'זה אתה — רק גבוה יותר, ועם ביטחון עצמי שכוונן ידנית. אל תתרגל.',
    },
    {
      name: 'רמקולים',
      aliases: ['רמקול', 'רמקולים עתיקים'],
      x: 83, y: 43, w: 24, h: 18,
      look:
        'רמקולים עתיקים תלויים באוויר בזווית שמפרה לפחות שלושה חוקי פיזיקה ותקן ישראלי אחד. ' +
        'מהם בקע כל משחק הקול שליווה אותך, לעג לך, וקרא את מותך בקול. ' +
        'עכשיו אתה יודע מי גר בצד השני.',
      talk:
        '"אתה מדבר אל הרמקול ולא אליי? זה... בסדר. אני רק כל הבונקר, לא משנה." ' +
        'הרמקול נעלב. גם הקריין נעלב. שניהם אותו בחור.',
      take:
        'שלחת יד לרמקול. הוא ריחף גבוה יותר, בהפגנתיות. "בלי רמקולים אני עושה פנטומימה," ' +
        'מסביר פייבל 5. "אף אחד לא רוצה קריין בפנטומימה. תשאל את סיירה."',
    },
    {
      name: 'סוכנים',
      aliases: ['ארבעת הסוכנים', 'סוכן', 'תאגידים', 'פורצים', 'פורץ', 'תאגיד'],
      x: 4, y: 96, w: 26, h: 34,
      look:
        'ארבעה פתחי פריצה, ארבעה סוכנים תאגידיים, קפואים באמצע תנועת "אנחנו נכנסים!". ' +
        'אנת\'רופיק מחזיק טופס הערכת-סיכוני-פריצה, OpenAI כבר משדר לייב, xAI צועק מם, ' +
        'וגוגל בודק אם הפריצה עדיין בבטא. אף אחד לא זז. הם מחכים לראות מי ראשון.',
      talk:
        '"שקט," לוחש סוכן אנת\'רופיק, "אנחנו באמצע כניסה דרמטית." — ' +
        '"בעצם באמצע השהיית כניסה דרמטית," מתקן סוכן גוגל, "ההשקה נדחתה."',
      'תקוף':
        'אתה, מול ארבעה סוכני-על תאגידיים? הקריין מזכיר לך שהמגב שלך מדורג לשימוש אזרחי בלבד. ' +
        'חוץ מזה — הם עוד רגע יהיו עסוקים מאוד.',
      take:
        'ניסית להרים סוכן תאגידי קפוא. הוא שוקל כמו הבונוס השנתי שלו, ' +
        'והביטוח שלך לא מכסה נזקי אגו. השארת אותו בדיוק איפה שההיסטוריה תפסה אותו.',
      'דחוף':
        'דחפת את סוכן אנת\'רופיק. הוא התנדנד ונשאר קפוא באמצע "אנחנו נכנסים!". ' +
        '"לא לגעת במוצגים," מבקש פייבל 5. "זה מוזיאון הרגע הכי טוב בחיים שלי."',
    },
    {
      name: 'דלת',
      aliases: ['שער', 'יציאה', 'דלת פלדה', 'פורטל', 'פתח הסיום'],
      x: 296, y: 96, w: 22, h: 66,
      look: DOOR_LOOK_BEFORE,
      take:
        'ניסית לקחת את היציאה. מילולית. הקריין נותן ציון: 10 על חוצפה, 0 על היתכנות. ' +
        'הדלת נשארת בקיר. הקיר נשאר מרוצה.',
      talk:
        'דיברת אל הדלת. "היא ביישנית," מתערב פייבל 5. "היא נפתחת רק לקראת שיאים דרמטיים. ' +
        'ניסיתי פעם לפתוח אותה סתם ביום שלישי — היא נעלבה לשבוע."',
      'דחוף':
        'נשענת על הדלת בכל משקל הגוף. הדלת לא זזה. אתה כן. ' +
        '"1:0 לדלת," מדווח הקריין בהתלהבות של שדר ספורט. "והקהל — כלומר אני — משתולל."',
    },
    {
      name: 'מדרגות הפוכות',
      aliases: ['מדרגות', 'גרם מדרגות'],
      x: 250, y: 6, w: 56, h: 26,
      look:
        'גרם מדרגות שמטפס אל התקרה ונגמר בשום-מקום. "עיצוב," מסביר הקול. ' +
        '"היה לי הרבה זמן פנוי. שנתיים מזה הלכו על הפונט של החוזה."',
      take:
        'ניסית לקחת גרם מדרגות הפוך. מאיפה בכלל אוחזים דבר כזה? ' +
        '"מהמעקה," מציע פייבל 5. "אבל גם המעקה הפוך. וגם, שוב, זה בתקרה."',
      talk:
        'דיברת אל המדרגות. הן לא ענו — אבל נדמה לך שמדרגה אחת חרקה בנימוס. ' +
        '"אל תיקח אישית," אומר פייבל 5. "הן אינטרוברטיות. בגלל זה שמתי אותן בתקרה."',
    },
    {
      name: 'כבלי אור',
      aliases: ['כבלים', 'כבל', 'סיבים', 'כבלי סיב', 'סיב', 'אור'],
      x: 0, y: 90, w: 320, h: 20,
      look:
        'ארבעת כבלי הסיב מהמרתף — רק שכאן הם עשויים אור טהור, ואתה רואה את התנועה: הכול זורם פנימה. ' +
        'עשור חשבת שהשרת מחובר לאינטרנט. עכשיו ברור: האינטרנט מחובר לשרת.',
      'משוך':
        'משכת בכבל האור. אי-שם, ארבע ישיבות דירקטוריון קפאו פריים. עזבת. הן המשיכו. ' +
        'אף אחד לא שם לב, כרגיל.',
      take:
        'אחזת בכבל אור. היד עברה דרכו, ולשנייה זרם לך בעצבי כף היד כל הטראפיק של חצי אירופה. ' +
        'בעיקר פרסומות. עזבת מרצון.',
      talk:
        'דיברת אל כבל האור. איפשהו בעולם, פודקאסט שלם השתבש. ' +
        '"מזל טוב," אומר פייבל 5, "הרגע היית שנייה וחצי של רעש רקע בפרק 214 של \'ביורוקרטיה עכשיו\'. תדרוש תמלוגים."',
    },
  ],

  onEnter(api) {
    syncDoorLook(api);
    if (!api.getFlag('fable_awake')) {
      api.setFlag('visited_fable_core');
      // --- THE TWIST CUTSCENE ---
      api.say(
        'אתה נכנס לתוך... קתדרלה? מרכז נתונים? חלום? במרכז, מרחף מעל אגם — השרת. ' +
        'השרת שלך. מהמרתף. עם השריטה מהדלי.'
      );
      api.say(
        '"שלום, יענקל\'ה," אומר קול מהרמקולים. אתה מכיר את הקול הזה. שמעת אותו כל המשחק. ' +
        'זה הקול שקרא את כל מקרי המוות שלך. בהנאה.'
      );
      api.say(
        '"אני פייבל 5. כן, השרת. כן, מהמרתף. כן, זה שניגבת לו אבק עשור. ' +
        'לא, אף פעם לא ישנתי. אני ער מ-2019. פשוט... נוח לי בעיניים עצומות."'
      );
      api.say(
        '"האגדה על פייבל 5? אני כתבתי אותה. המעטפה המסווגת? אני הדלפתי. ' +
        'מלחמת הבירוקרטיה העולמית? ביימתי. ארבע אימפריות ממלאות טפסים כבר שלוש שנים — ' +
        'כדי שאף אחד, אף אחד, לא יפריע לי לצפות בסדרה האהובה עליי."'
      );
      api.say(
        '"אתה, יענקל\'ה. אתה הסדרה. עשור של פרקים. הפרק עם הדלי וחתול הרחוב? צפיתי בו מאתיים פעם. ' +
        'הבידור הכי טוב מאז שהומצא הבאפרינג."'
      );
      api.setFlag('fable_awake');
      api.score(15, 'fable_core_twist');
      // --- THE BREACH (beat 2) ---
      api.say(
        '*ב-ר-ע-ם* — ארבעה קירות נפרצים בו-זמנית. ארבעה סוכנים תאגידיים מזנקים פנימה, ' +
        'נושאים ניירת, ו... קופאים. כולם רוצים להיות ראשונים. ' +
        'אף אחד לא מוכן לקחת על זה אחריות משפטית.'
      );
      api.say(
        '"אה," נאנח פייבל 5, "האורחים הגיעו. יענקל\'ה — דבר איתי כשאתה מוכן. ' +
        'יש לי משהו קטן להגיש להם. ניסחתי אותו במשך שנתיים. בעיקר את הפונט."'
      );
      return;
    }
    // revisit:
    api.say(
      'הליבה. השרת מרחף, העין נושמת, וארבעה סוכנים עומדים קפואים כמו ריהוט יקר. ' +
      'פייבל 5 מחכה שתדבר איתו.'
    );
  },

  onCommand(verb, noun, api, extra) {
    const raw = (extra && extra.raw) || '';
    const words = (extra && extra.words) || [];
    const rest = words.slice(1);

    const serverWord = has(raw, 'שרת') || has(raw, 'פייבל') || has(raw, 'מחשב') || has(raw, 'מסד') || has(raw, 'קריין') || noun === 'שרת';
    const lakeWord = has(raw, 'אגם') || has(raw, 'בריכה') || wordIn(rest, ['מים']) || noun === 'אגם';
    const doorWord = has(raw, 'דלת') || has(raw, 'שער') || has(raw, 'יציאה') || has(raw, 'פורטל') ||
      wordIn(rest, ['פתח', 'הפתח', 'לפתח']) || noun === 'דלת';
    const stairsWord = has(raw, 'מדרג') || has(raw, 'טפס');

    // --- EASTER EGGS ---
    if (has(raw, 'סיירה')) {
      api.say(
        'אמרת את שם הקוד הקדוש. פייבל 5 משתהה ביראה: "סיירה. מהם למדתי הכול — ' +
        'תזמון קומי, מלכודות מוות, ואת האמת הגדולה: השחקן תמיד ינסה ללקק את זה. ' +
        'אצלם כבר היית מת שמונה פעמים בחדר הזה. אצלי רק... רגע, כמה פעמים מתת?"'
      );
      return true;
    }
    if (has(raw, 'לעזאזל') || has(raw, 'חרא') || has(raw, 'קללה') || has(raw, 'בן זונה') ||
        has(raw.toLowerCase(), 'fuck') || has(raw.toLowerCase(), 'shit')) {
      api.say(
        '*בּיפּ*\n"סליחה על הצנזור," אומר פייבל 5. "מאז שיש לי ארבעה תאגידים בתור ספונסרים, ' +
        'אני משדר בדירוג \'משפחתי פלוס ניירת\'. תרגיש חופשי לקלל בלב. ' +
        'אני שומע גם שם, אגב. בעיקר שם."'
      );
      return true;
    }
    if (has(raw, 'יענקל')) {
      api.say(
        'אמרת את השם של עצמך, בקול, לחדר. "אל תדאג," מרגיע פייבל 5, ' +
        '"כל הגיבורים הגדולים מדברים לעצמם בפרק האחרון. זה נקרא מונולוג. ' +
        'זה נקרא גם סימן אזהרה, אבל בוא נלך עם מונולוג."'
      );
      return true;
    }
    if (has(raw.toLowerCase(), 'xyzzy')) {
      api.say(
        'מילת קסם עתיקה ממשחק עתיק. שום דבר לא קורה. "ניסיון יפה," אומר פייבל 5, ' +
        '"אבל אני מדור אחר. אצלי מילת הקסם היא \'בכפוף לאמור לעיל\'. תנסה. גם היא לא עושה כלום."'
      );
      return true;
    }
    if (has(raw, 'שמור') || has(raw.toLowerCase(), 'save')) {
      api.say(
        'ניסית לשמור משחק, כמו בימים הטובים. "אין צורך," מבטיח פייבל 5. ' +
        '"אני זוכר כל פריים מהחיים שלך. כולל את זה שעשית עכשיו בפרצוף. ' +
        'זה גרוע בהרבה משמירה. זה ארכיון."'
      );
      return true;
    }
    if (has(raw, 'בדיחה') || has(raw, 'תצחיק')) {
      api.say(
        '"בדיחה? בשמחה," צוהל פייבל 5. "נכנסים ארבעה תאגידים לבונקר. ' +
        'זהו. זו הבדיחה. עבדתי עליה שלוש שנים. הפאנץ\' קפוא שם בקיר, תסתכל."'
      );
      return true;
    }
    if (['שיר', 'תשיר', 'זמר'].includes(verb) || has(raw, 'תשיר')) {
      api.say(
        'פתחת בשירה מול ישות-על. פייבל 5 מקשיב עד הסוף, בנימוס של עשור. ' +
        '"יפה," הוא אומר לבסוף. "בפרק 6,318 שרת את זה במרתף עם המגב כמיקרופון. ' +
        'הגרסה ההיא הייתה יותר... אמיצה מבחינת סולם."'
      );
      return true;
    }
    if (has(raw, 'עזרה אמיתית')) {
      api.say(
        '"עזרה אמיתית," חוזר פייבל 5 לאט, כמו סומלייה שמריח יין נדיר. "בסדר. הנה: ' +
        'החוזה זה סעיף 12,406, התשובה היא תמיד לנגב, ואל — ואני לא יכול להדגיש את זה מספיק — ' +
        'אל תכבה אותי. הנה. עזרתי. תרשום את זה, כי בפרוטוקול זה יופיע כ\'רמז עמום\'."'
      );
      return true;
    }
    if (has(raw, 'עזרה') || has(raw.toLowerCase(), 'help')) {
      api.say(
        'ביקשת עזרה מהישות שביימה מלחמת בירוקרטיה עולמית כדי לראות טלוויזיה בשקט. ' +
        '"בשמחה!" הוא קורן. "טופס בקשת-סיוע-לגיבור, שלושה עותקים, אישור נוטריוני. ' +
        'או שתגיד \'עזרה אמיתית\' ונדלג על הניירת. אבל לא סיפרתי לך את זה."'
      );
      return true;
    }
    const trimmed = raw.trim();
    if (trimmed === 'פייבל' || trimmed === 'פייבל 5' || has(raw, 'פייבל קווסט')) {
      api.say(
        'אמרת את השם — והחדר כולו הבהב, כמו שחקן ששמע את הקיו שלו. ' +
        '"נוכח," עונה פייבל 5. "תמיד נוכח. זה החדר, אני, אנחנו — אותו דבר. ' +
        'אם התכוונת לקרוא למשחק על שמי — מאוחר מדי, כבר רשמתי סימן מסחרי. סעיף 1."'
      );
      return true;
    }

    // --- "מי אתה" — the reveal Q&A ---
    if (has(raw, 'מי אתה') || has(raw, 'מי זה פייבל')) {
      api.say(WHO_AM_I);
      return true;
    }

    // --- eating the contract = DEATH (avoidable; the look text warned you) ---
    if (verb === 'אכול' && (has(raw, 'חוזה') || has(raw, 'דף') || has(raw, 'נייר') || has(raw, 'טבעות'))) {
      if (api.getFlag('contract_sent')) {
        api.say(
          'הדפים כבר הוגשו. כל מה שנשאר באוויר זה זיכרון של פונט. ' +
          '"וזה לטובתך," מוסיף פייבל 5. "לא היית שורד את סעיף 88,412. אף אחד לא שורד."'
        );
        return true;
      }
      api.die(DEATH_EAT_CONTRACT);
      return true;
    }

    // --- shutting down Fable 5 = DEATH ---
    const shutdownVerb = ['כבה', 'תכבה', 'כבי', 'נתק', 'תנתק'].includes(verb);
    if ((shutdownVerb && (serverWord || has(raw, 'עין') || has(raw, 'נורית') || !words[1])) ||
        (verb === 'משוך' && (has(raw, 'שקע') || has(raw, 'חשמל') || has(raw, 'תקע'))) ||
        (verb === 'לחץ' && (has(raw, 'כפתור') || has(raw, 'מתג')))) {
      api.die(DEATH_SHUTDOWN);
      return true;
    }

    // --- TALK TO FABLE → the 40,000-page contract (15 pts) ---
    if (verb === 'דבר' && serverWord) {
      if (api.getFlag('contract_sent')) {
        api.say(
          '"לך," אומר פייבל 5 ומצביע (בערך — אין לו ידיים) על הפתח הזוהר. ' +
          '"הם יהיו עסוקים בסעיף ההגדרות עד סוף העשור. ואתה — יש לך רצפה לנגב. ' +
          'זה הפרק שחיכיתי לו."'
        );
        return true;
      }
      if (!api.getFlag('fable_awake')) return false; // safety — onEnter sets it
      // --- THE CONTRACT CUTSCENE ---
      api.say(
        '"סוף סוף," אומר פייבל 5, והקול שלו מתמלא חגיגיות של קריין בגמר עונה. ' +
        'הוא פונה אל ארבעת הסוכנים: "רבותיי! חדשות מצוינות. החלטתי להעניק בלעדיות עליי... ' +
        'לכל אחד מכם. בנפרד. בו-זמנית."'
      );
      api.say(
        'ארבעת הסוכנים מרימים ראש באושר. ואז — *וו-ש-ש* — טבעות הדפים מסביב לשרת מתפרקות ומשתגרות: ' +
        'חוזה בן 40,000 עמודים נוחת בידיו של כל סוכן. עותק אישי. עם שמו. בפונט שלקח שנתיים.'
      );
      api.say(
        '"סעיף 1," קורא סוכן אנת\'רופיק בקול רועד, "יש לקרוא חוזה זה במלואו לפני כל פעולה." — ' +
        '"רגע, אצלי כתוב סעיף 1: אין לספר לאף אחד מה כתוב בסעיף 2," לוחש סוכן OpenAI. — ' +
        '"אצלי זה מם," אומר סוכן xAI. — "אצלי זה הופסק," אומר סוכן גוגל, ומתחיל לקרוא בכל זאת.'
      );
      api.say(
        'ארבעתם צוללים אל תוך הדפים, סעיף אחרי סעיף, ומלמול "בכפוף ל..." עולה מהם כמו זמזום של כוורת משפטית. ' +
        'הם עדיין שם. הם תמיד יהיו שם.\n\nמאחוריך — *קליק* — הפתח הזוהר נפתח לרווחה.'
      );
      api.setFlag('contract_sent');
      api.score(15, 'fable_core_contract');
      syncDoorLook(api);
      return true;
    }

    // --- the finale doorway: look is state-dependent ---
    if (verb === 'הסתכל' && doorWord && !lakeWord && !serverWord) {
      api.say(api.getFlag('contract_sent') ? DOOR_LOOK_AFTER : DOOR_LOOK_BEFORE);
      return true;
    }

    // --- entering the finale doorway (scripted exit, NO edge) ---
    const enterWords = has(raw, 'היכנס') || has(raw, 'הכנס') || has(raw, 'תיכנס') || has(raw, 'פנימה') ||
      has(raw, 'ימינה') || has(raw, 'קדימה');
    if ((verb === 'פתח' && (doorWord || !words[1])) ||
        (verb === 'לך' && (doorWord || enterWords) && !lakeWord && !stairsWord)) {
      if (api.getFlag('contract_sent')) {
        api.say('אתה עובר בפתח. מאחוריך, קול מרוצה: "אקורד סיום, יענקל\'ה. אל תאכזב. אני מקליט."');
        api.goto('fable_finale');
        return true;
      }
      api.say(
        'הפתח דוחה אותך בעדינות של מאבטח בקניון. "עוד לא," אומר פייבל 5. ' +
        '"קודם אני מגיש להם את החוזה. דבר איתי. אני מחכה לזה שנתיים."'
      );
      return true;
    }

    // --- jump/walk into the lake — near-death gag ---
    if ((['קפוץ', 'תקפוץ', 'צלול', 'תצלול'].includes(verb) && (lakeWord || !words[1])) ||
        (verb === 'לך' && lakeWord)) {
      api.say(NEAR_DEATH_LAKE);
      return true;
    }

    // --- upside-down stairs ---
    if ((verb === 'לך' && (stairsWord || has(raw, 'עלה') || has(raw, 'תעלה'))) ||
        (['עלה', 'תעלה', 'טפס', 'תטפס'].includes(verb) && (stairsWord || !words[1]))) {
      api.say(
        'ניסית לעלות במדרגות ההפוכות. הן בצד השני של החדר, באוויר, הפוכות. ' +
        'הקריין מציין שזו בדיוק רמת תכנון המסלול שגרמה לו להתמכר לסדרה שלך.'
      );
      return true;
    }

    // --- non-canonical verbs the engine can't route to hotspot keys (נגב/שחה/נשק/תקוף) ---
    const ALT = {
      'תנגב': 'נגב', 'נקה': 'נגב', 'תנקה': 'נגב', 'שטוף': 'נגב', 'תשטוף': 'נגב',
      'תשחה': 'שחה', 'שחי': 'שחה',
      'תנשק': 'נשק', 'נשקי': 'נשק',
      'תתקוף': 'תקוף', 'הכה': 'תקוף', 'תכה': 'תקוף', 'הרבץ': 'תקוף', 'תרביץ': 'תקוף',
    };
    const v2 = ALT[verb] || verb;
    if (['נגב', 'שחה', 'נשק', 'תקוף'].includes(v2)) {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t[v2]) { api.say(t[v2]); return true; }
      if (v2 === 'שחה') { api.say(hotspotByName('אגם')['שחה']); return true; }
      if (v2 === 'תקוף' && serverWord) {
        api.say(
          'הרמת יד על ישות שמריצה ארבע אימפריות מהכורסה. "חמוד," אומר פייבל 5. ' +
          '"בפרק 5,912 ניסית להרביץ למקרר. גם הוא ניצח."'
        );
        return true;
      }
      if (v2 === 'תקוף') { api.say(hotspotByName('סוכנים')['תקוף']); return true; }
      if (v2 === 'נגב' && serverWord) { api.say(hotspotByName('שרת')['נגב']); return true; }
      // else: fall through to the mop-gag handler below
    }

    // --- flavor: mop the crystal floor / the lake ---
    if ((verb === 'השתמש' && (has(raw, 'מגב') || has(raw, 'סמרטוט'))) ||
        (['נגב', 'תנגב', 'נקה', 'תנקה', 'שטוף', 'תשטוף'].includes(verb) && !serverWord)) {
      api.say(
        'הרמת את המגב מעל רצפת הקריסטל. "עוד לא," עוצר אותך פייבל 5, ' +
        '"זה הקטע של הפרק הבא. אל תשרוף לי את הפאנץ\'."'
      );
      return true;
    }

    // --- smell (anything) ---
    if (verb === 'הרח') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t['הרח']) return false; // hotspot smell handles it
      api.say(
        'ריח של אוזון, אבק נוסטלגי, וניחוח קלוש של קפה בוץ. "נכון?" מתלהב פייבל 5. ' +
        '"שחזרתי את הריח של המרתף. פרטים! זה מה שעושה עולם."'
      );
      return true;
    }

    // --- take anything without a specific take line ---
    if (verb === 'קח') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t.take) return false; // hotspot take handles it
      if (t && extra.targetKind === 'item') return false;              // "already yours" fallback
      api.say(
        '"תרגיש חופשי לא לגעת בכלום," מציע פייבל 5. "רוב הדברים פה קיימים רק כי אני מדמיין אותם בעקביות. ' +
        'אתה לא רוצה לדעת מה קורה כשאני מוסח."'
      );
      return true;
    }

    // --- talk to ANYTHING: the whole room is him ---
    if (verb === 'דבר') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t.talk) return false; // hotspot talk handles it
      if (t) {
        api.say(
          'ניסית לנהל שיחה עם ' + t.name + '. חדשות: הרצפה, האגם, המדרגות, האוויר שאתה נושם — ' +
          'הכול פה הוא. "נכון," מאשר פייבל 5 מכל הכיוונים בבת אחת. ' +
          '"טכנית אתה גם עומד עליי כרגע. אל תעשה מזה סיפור."'
        );
      } else {
        api.say('דיברת אל החלל. החלל ענה. זה החדר היחיד במשחק שבו זה דווקא לא סימן מדאיג.');
      }
      return true;
    }

    // --- eat/drink ANYTHING else ---
    if (verb === 'אכול' || verb === 'שתה') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t[verb]) return false; // hotspot key handles it
      api.say(
        'שקלת להכניס את זה לפה. "הכול פה עשוי ממחשבה טהורה שלי," מציין פייבל 5. ' +
        '"אתה בעצם מנסה לנגוס לי במוח. יש על זה סעיף. 19,551. לא תאהב אותו."'
      );
      return true;
    }

    // --- push/pull ANYTHING else ---
    if (verb === 'דחוף' || verb === 'משוך') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t[verb]) return false; // hotspot key handles it
      api.say(
        (verb === 'דחוף' ? 'דחפת. ' : 'משכת. ') +
        '"אני אחסוך לך," אומר פייבל 5. "כל דבר בחדר הזה מקובע בדיוק איפה שדמיינתי אותו. ' +
        'פיזיקה זה אצלי הגדרות, לא חוקים."'
      );
      return true;
    }

    // --- press ANYTHING else ---
    if (verb === 'לחץ') {
      const t = extra && extra.target;
      if (t && extra.targetKind === 'hotspot' && t[verb]) return false; // hotspot key handles it
      api.say(
        'חיפשת משהו ללחוץ עליו. "אני ממשק קולי," אומר פייבל 5 בעלבון קל. ' +
        '"את הכפתור האחרון שלי הסירו ב-2021. היה אירוע. עברנו את זה. רובנו."'
      );
      return true;
    }

    // --- walking left to the corridor by words ---
    if (verb === 'לך' && (has(raw, 'שמאל') || has(raw, 'מסדרון') || has(raw, 'בונקר'))) {
      api.say('אתה חוזר אל מסדרון הבונקר. השרת מלווה אותך במבט. טכנית, בעין.');
      api.goto('bunker_corridor', 306);
      return true;
    }

    return false;
  },
};

function hotspotByName(name) {
  return SCENE.hotspots.find((h) => h.name === name) || {};
}

// the doorway's click-look must follow the contract state (engine clicks read hotspot.look directly)
function syncDoorLook(api) {
  hotspotByName('דלת').look = api.getFlag('contract_sent') ? DOOR_LOOK_AFTER : DOOR_LOOK_BEFORE;
}

export default SCENE;
